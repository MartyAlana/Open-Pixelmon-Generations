--- a/com/pixelmongenerations/client/gui/trashcan/ContainerTrashcan.java
+++ b/com/pixelmongenerations/client/gui/trashcan/ContainerTrashcan.java
@@ -14,8 +14,8 @@
 
         int x;
         for(x = 0; x < 3; ++x) {
-            for(int x = 0; x < 9; ++x) {
-                this.addSlotToContainer(new Slot(player, x + x * 9 + 9, 8 + x * 18, 84 + x * 18));
+            for(int y = 0; y < 9; ++y) {
+                this.addSlotToContainer(new Slot(player, x + x * 9 + 9, 8 + x * 18, 84 + y * 18));
             }
         }
 
